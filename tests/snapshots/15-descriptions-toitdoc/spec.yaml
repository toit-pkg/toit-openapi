openapi: 3.1.0
info:
  title: Descriptions
  version: 1.0.0
paths:
  /search:
    get:
      operationId: search
      description: Searches the catalog and returns matching items.
      tags: [items]
      parameters:
        - name: q
          in: query
          required: true
          description: Free-text search query.
          schema:
            type: string
      responses:
        '200':
          description: OK
